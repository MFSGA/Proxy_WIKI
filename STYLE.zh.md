# Proxy Wiki 风格指南

Proxy Wiki 是 Chimera 代理技术栈的技术参考文档。写作目标是让读者准确理解行为、
配置方式和兼容性边界。

## 写作原则

- 优先描述具体行为，避免宣传式表达。
- 区分已确认实现、部分实现和计划中的能力。
- 讲配置格式或协议流程时，尽量给出示例。
- 术语应与相关项目保持一致：`Chimera_Client`、`Chimera`、
  `Chimera_Server`、Clash/Mihomo、xray-core。
- 只有在描述协议要求或外部规范时才使用 `MUST`、`SHOULD`、`MAY`。

## 结构

- 面向用户的安装、配置和运行步骤应放在实现细节之前。
- 面向开发者的内容应尽量写明模块、文件或 API 路径。
- 已知限制应放在对应功能章节中；跨模块问题汇总到 `src/open-questions.md`。
- 协议页面应覆盖握手形态、配置表面、互通说明、安全注意事项和已知限制。

## 代码和配置块

- 尽量使用带语言标记的 fenced code block。
- 示例密钥、UUID 和域名必须明显是占位值。
- 优先使用最小可运行示例，不要复制过大的配置。
- 命令只适用于特定平台时，应注明 Linux、macOS 或 Windows 前提。

## 翻译

- 协议名、配置键、命令和文件路径保持不变。
- 翻译解释性文字，不翻译机器读取的值。
- 如果英文原文含混，应先修英文源文档，再更新翻译。
